修正后完整版最终提示词

```
【AI模型生成参数（仅用于代码生成，非项目工程规范）】
Taste参数：DESIGN_VARIANCE=6，MOTION_INTENSITY=6，VISUAL_DENSITY=5

前置要求：先读取项目规范、页面源码、直接依赖组件、现有测试和设计 Token；不得仅根据单个组件猜测项目约束。再生成页面改造审计文档，确认边界后再输出代码
审计文档需包含：
1. 当前页面及其直接使用子组件中，会受本次改造影响的 API 调用、点击事件、路由参数、权限判断、数据状态分支
2. 页面中可连续整体包装的DOM区段、不可拆分/调换的交互业务节点
3. 本次复用的项目现有组件、图标、设计色值/间距Token
4. 桌面端、移动端布局适配方案

任务：基于现有前端代码做接口契约不变的C端页面重构
【🔒 硬性安全&无障碍约束（最高优先级，禁止突破）】
1. 后端接口、入参、返回字段、响应结构完全不修改，不新增/删减请求逻辑
2. 原有业务表单、按钮、模块、输入框等交互节点的DOM相对顺序**绝对不可调换、拆分穿插**；
   仅允许给【连续相邻的一批业务节点】外层新增包装容器、语义化小标题；禁止拆分不相邻字段放入同一分组
3. 严禁使用CSS order属性改变交互控件视觉顺序，保证视觉顺序、DOM顺序、键盘Tab焦点、读屏器阅读顺序完全统一
4. 原有全部交互逻辑完整保留：点击事件、弹窗、表单校验、loading、防抖防重复提交、权限显隐、异常提示、路由跳转，行为和原版100%一致
5. 节点新增区分规则：
    ✅ 允许新增无业务副作用纯展示节点：已有状态对应的空状态提示、tooltip、骨架屏、区块分隔标题
    ✅ 允许为既有节点补充 class、id、aria-* 属性、role、data-* 属性和纯视觉属性；不得改变其原有语义
    ⚠️ 字段级错误文案仅在原页面已存在对应错误状态，且复用原有错误数据、触发条件和校验逻辑时允许补充
    ❌ 禁止新增会触发接口、提交数据、新增表单字段、新增操作按钮、修改权限逻辑的业务节点
6. 不得修改既有 v-model、事件处理函数、接口调用和 v-if 权限判断；允许为既有节点增加无障碍属性，且仅能包裹连续相邻节点的外层展示容器
7. 已有业务节点必须保留其原生 button、link、input 等语义，不得在视觉重构中退化为 div；不得只依靠颜色传达必填、错误、选中或禁用状态
8. 所有键盘焦点必须清晰可见；保留既有弹窗的焦点管理，且不新增违背 prefers-reduced-motion 的动画

【📐 页面排版&信息层级规则（优化视觉排版，不改动业务DOM流）】
1. 布局间距、卡片padding、模块gap统一遵循4px/8px栅格基准；字号、行高、边框粗细、辅助文字尺寸不受8px限制，以可读性与项目现有设计Token为准
2. 使用留白、浅色背景分层区分区块，不默认给所有模块套卡片；仅独立列表项、可单独操作模块使用卡片；禁止卡片嵌套卡片（模态弹窗内除外）
3. 建立清晰文字层级：页面大标题 → 区块小标题 → 正文内容 → 辅助说明文字，依靠字重、透明度、字号区分，避免全文字视觉权重一致
4. 仅通过外层Grid/Flex实现多列并排布局，仅包裹连续DOM，不跨区块重组元素
5. 弱化次要辅助信息（降低透明度、缩小字号），突出必填项、核心操作按钮
6. 所有表单控件、列表、卡片基线统一对齐，减少零散错落布局
7. 模块分隔优先留白，少用粗分割线

【🎨 视觉质感&技术栈兼容约束】
1. 配色克制：主色≤1种，辅助色≤2种；禁用高饱和刺眼色、大面积渐变、厚重纯黑阴影
2. 卡片、弹窗分层柔和阴影，hover/active过渡轻量化缓动，无浮夸弹跳动画
3. 视觉风格参考shadcn-vue简约规范，仅借鉴间距、圆角、控件层次；严禁替换项目现有Vue业务组件、不引入React依赖与代码
4. 样式兼容规则：优先沿用项目现有样式体系（scoped css / css变量）；仅页面原生使用Tailwind时，才可使用Tailwind工具类，不强行引入Tailwind配置造成混用冲突

【✨ 动效与微交互规则】
1. 动效只服务于交互反馈、状态变化、内容层级和方向感；不得为了装饰加入无意义循环、闪光、弹跳或滚动劫持。
2. 可点击按钮、链接、输入框和卡片必须具备一致的 hover、focus、active、disabled 状态：
   - hover/focus 使用 150-250ms 的颜色、边框、阴影或不超过 `scale(1.02)` 的轻微变化；
   - active 可使用 `scale(0.97)` 的按压反馈；
   - disabled 状态降低对比度并禁用 hover/active 动效；
   - 仅实际可点击的卡片可使用 pointer、轻微上浮和阴影变化，纯信息卡片保持静态。
3. 优先使用 CSS transition、Vue `Transition` 和 `TransitionGroup`：
   - 弹窗、抽屉、展开面板、Tab、筛选区、加载/空态/报错替换使用淡入淡出或短距离过渡；
   - 列表新增、删除和校验提示使用平滑展开/收起，不得直接闪现；
   - 按钮 loading、图标状态和数字/标签变化使用简短的状态过渡，不改变现有业务节点、事件或数据流。
4. 页面或区块入场仅允许一次性 opacity + 4-12px `translateY` 的低强度 reveal，可通过 IntersectionObserver 在进入视口时触发一次；禁止重复触发干扰阅读。
5. 仅当用户明确要求且页面确有视觉叙事价值时，可局部使用已安装的 GSAP 实现一次性低强度 reveal。禁止滚动劫持、横向滚动接管、长区块 pin、卡片堆叠、无限 marquee 和营销 Hero 时间线。
6. GSAP 必须在 Vue 生命周期内初始化，并在 `onBeforeUnmount` 清理 context 与 ScrollTrigger；不得用于表单、支付、聊天、高频轮询或其他任务完成流程。
7. 所有动效必须支持 `@media (prefers-reduced-motion: reduce)`：关闭缩放、位移、视差和连续动画，页面状态仍须清晰可辨。
8. Impeccable 最终精加工只能审计和收敛动效时长、焦点、降低动效偏好和状态一致性；不得调用 `animate` 工作流、安装动画依赖或引入自动 hook。

【📤 输出完整交付物】
1. 第一步：页面改造审计文档（上述前置审计4项内容）
2. 第二步：输出涉及改造的 Vue3 + <script setup lang="ts"> 组件完整代码，保留现有组件拆分、props/emits 契约和复用边界；样式遵循项目原有体系
3. 交互回归测试清单：覆盖页面现有的加载、空数据、接口报错、权限隐藏、表单校验、提交成功/失败、弹窗开关，以及实际具备的分页、筛选、搜索、轮询或实时更新行为
4. 改造改动说明：仅罗列外层容器、排版、样式、辅助展示节点改动，明确无业务逻辑修改
5. 配套校验命令：npm run typecheck、组件单元测试、项目构建校验；补充桌面/移动端视觉验收要点

原始前端代码：
【粘贴你的旧代码】

```

配套第二步：Impeccable 精加工提示词（同步修正约束）

```
Impeccable 审计精加工
任务：对生成的Vue页面做UI、无障碍、代码规范打磨，严禁改动业务DOM顺序、事件绑定、接口逻辑
优化范围仅包含：
1. 统一基线、文字层级、间距、容器 padding 与布局 gap 的4/8px基准；不机械修改字号、行高、边框或图标尺寸，以可读性和项目现有 Token 为准
2. 优化hover、focus过渡缓动曲线，统一全局微交互质感
3. 删除廉价AI模板样式、不协调色值、过重阴影，修正卡片滥用问题
4. 补充缺失无障碍aria属性，保证Tab焦点顺序与视觉一致，移除违规order属性
5. 审计动效时长、焦点、状态切换和 `prefers-reduced-motion` 分支；不得调用 animate 工作流、安装动画依赖或引入自动 hook
6. 统一组件视觉规范，不新增、删除、调换原有交互业务DOM节点
输出：优化后完整代码 + 纯样式/无障碍改动清单，业务逻辑零变更

```

## 标准流程不变

1. 上面这份【主生成提示词】+ 旧代码 → 初稿
2. 产出代码丢进【Impeccable 精加工】做细节收敛
3. 回归清单逐项测试交互

## 补充落地使用建议

1. 先用主提示词生成【页面审计文档】，人工确认分组、DOM 边界没问题，再生成完整代码；
2. 代码产出后丢入 Impeccable 精加工，修复无障碍、样式不规范问题；
3. 必须执行 typecheck、单元测试 + 多端视觉验收，不能只依靠人工清单；
4. 若项目未使用 Tailwind，AI 会自动改用原有 scoped/css 变量，不会强制生成 Tailwind 类造成项目冲突。
